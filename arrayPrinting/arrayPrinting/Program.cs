// Use the foreach to print the contents of the "merkit" table.

using System;
class Program
{
    static void Main()
    {

        char[] merkit = { 'O', 'p', 'i', ' ', 'C', '#', '!' };

        foreach (int merkki in merkit)
        {
            Console.Write(merkit);
            return;
        }
    }
}


